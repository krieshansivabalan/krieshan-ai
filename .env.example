# ── Required ──────────────────────────────────────────────────────
# Generate a strong random key:  python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=change-me-to-a-long-random-string

# ── Google OAuth (get from console.cloud.google.com) ─────────────
# Leave blank to use auto-login dev mode (everyone shares one account)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── Stripe (optional — for paid transit feature) ──────────────────
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ── Database (Railway/Render auto-set this for Postgres) ──────────
# Leave blank to use local SQLite
DATABASE_URL=
